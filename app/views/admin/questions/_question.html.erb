  <tr>
    <th><%= link_to question.body, admin_question_path(question.id) %></th>
    <td><%= link_to "Изменить", edit_admin_question_path(question.id) %></td>
    <td>
      <%= link_to "Удалить",
                  admin_question_path(question),
                  method: :delete,
                  data: { confirm: "Вы уверенны?" } %>
    </td>
  </tr>

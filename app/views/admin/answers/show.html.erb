<h1><%= @answer.question.body %></h1>

<h2><%= @answer.body %></h2>

<p><%= @answer.correct %></p>

<table>
  <tr>
    <td><%= link_to "Изменить", edit_admin_answer_path(@answer) %></td>
    <td>
      <%= link_to "Удалить",
                  admin_answer_path,
                  method: :delete,
                  data: { confirm: "Вы уверенны?" } %>
    </td>
    <td><%= link_to "Назад", admin_question_path(@answer.question_id) %></td>
  </tr>
</table>

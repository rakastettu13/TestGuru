<% if resource.present? && resource.errors.any? %>
  <%= tag.div id: "resource-errors", data: { resource_id: resource.id } do %>
      <% resource.errors.full_messages.each do |message| %>
        <p><%= message %></p>
      <% end %>
  <% end %>
<% end %>
